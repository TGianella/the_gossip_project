<!DOCTYPE html>
<html>
  <head>
    <title>TheGossipProject</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="navbar navbar-expand-sm bg-dark" data-bs-theme="dark">
      <div class="container-fluid">
        <a class="navbar-brand" href="#">The Gossip Project</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav me-auto mb-0 mb-lg-0">
            <li class="nav-item">
              <a class="nav-link active" aria-current="page" href="<%=gossips_path%>">Tous les potins</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="<%=team_path%>">Team</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="<%=contact_path%>">Contact</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="<%=new_gossip_path%>">Créer un potin</a>
            </li>
            <% if logged_in? %>
              <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false" data-turbo-disabled='true'>
                  <%= current_user.first_name %>
                </a>
                <ul class="dropdown-menu">
                  <li><a class="nav-link" href="<%=user_path(current_user)%>">Mon compte</a></li>
                  <li><%= link_to "Se déconnecter", logout_path, class: "nav-link", data: { turbo_method: :delete } %></li>
                </ul>
              </li>
            <% else %>
              <li class="nav-item">
                <a class="nav-link" href="<%=new_session_path %>">Se connecter</a>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>
    <%= render 'application/flashes' %>
    <div class="container mt-4">
      <%= yield %>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js" integrity="sha384-w76AqPfDkMBDXo30jS1Sgez6pr3x5MlQ1ZAGC+nuZB+EYdgRZgiwxhTBTkF7CXvN" crossorigin="anonymous"></script>
  </body>
</html>
